---

- hosts: usenet

  vars_files:
    - vars/services.yml

  roles:
    - ubuntu-setup
    - network-shares
    - docker
    - docker-network
    - nzbget-docker
    - transmission-docker
    - nzbhydra-docker
    - sonarr-docker
    - couchpotato-docker
    - muximux-docker
    - nginx-docker
    - letsencrypt-docker
    # - freenas-permissions
    # - centos-setup
    # - radarr-docker

# Just for setting IP addresses
- hosts: new-machine

  roles:
    - static-ip
